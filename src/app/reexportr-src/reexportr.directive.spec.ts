import { ReExportrDirective } from "./reexportr.directive";

describe("ReexportrDirective", () => {
  it("should create an instance", () => {
    const directive = new ReExportrDirective();
    expect(directive).toBeTruthy();
  });
});
